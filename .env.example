# VO Tools Environment Configuration

# ===========================================
# SECURITY CONFIGURATION (REQUIRED)
# ===========================================

# API Key for authentication - CHANGE THIS IN PRODUCTION!
# Generate a secure random key: openssl rand -hex 32
API_KEY=your-secure-api-key-change-this

# Enable/disable API key authentication
# WARNING: Only disable for local development, never in production
AUTH_ENABLED=true

# ===========================================
# CORS CONFIGURATION
# ===========================================

# Allowed origins for CORS (comma-separated)
# Production: ALLOWED_ORIGINS=https://voiceover-tools.com,https://www.voiceover-tools.com
# Development: ALLOWED_ORIGINS=http://localhost:3011,http://localhost:3000,http://dev.votools.tohareprod.com,https://dev.votools.tohareprod.com
# Combined example (dev + prod):
ALLOWED_ORIGINS=https://voiceover-tools.com,https://www.voiceover-tools.com,http://localhost:3010

# ===========================================
# API CONFIGURATION
# ===========================================

# Flask API URL (for Next.js to communicate with Python backend)
# Internal Docker network: http://localhost:5000
# External: your backend URL
FLASK_API_URL=http://localhost:5000

# Public-facing API URL (optional)
# Leave empty to use same-origin API
# Or set to your backend URL, e.g., http://localhost:5000
NEXT_PUBLIC_API_URL=

# ===========================================
# UPLOAD CONFIGURATION
# ===========================================

# Maximum upload size in bytes (default: 50MB)
MAX_CONTENT_LENGTH=52428800

# Upload folder path (default: /tmp/uploads)
UPLOAD_FOLDER=/tmp/uploads

# Allowed file extensions (comma-separated)
ALLOWED_EXTENSIONS=wav,mp3,ogg,flac,m4a,aiff,wma,aac

# FFmpeg timeout in seconds (default: 60)
FFMPEG_TIMEOUT=60

# ===========================================
# RATE LIMITING
# ===========================================

# Enable/disable rate limiting
RATE_LIMIT_ENABLED=true

# Requests per minute per IP
RATE_LIMIT_PER_MINUTE=10

# Requests per hour per IP
RATE_LIMIT_PER_HOUR=100

# WebSocket rate limit per minute (for teleprompter remote)
WS_RATE_LIMIT_PER_MINUTE=30

# ===========================================
# FLASK SERVER CONFIGURATION
# ===========================================

# Flask host (0.0.0.0 to accept all connections)
FLASK_HOST=0.0.0.0

# Flask port
FLASK_PORT=5000

# Flask debug mode (never enable in production)
FLASK_DEBUG=false

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ===========================================
# NEXT.JS CONFIGURATION
# ===========================================

# Node environment
NODE_ENV=production

# CORS max age in seconds
CORS_MAX_AGE=3600
